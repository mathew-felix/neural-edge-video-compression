# Locked Python package set for the FFmpeg-only compression/decompression pipeline.
# Include CUDA torch wheels so a single pip -r command works on GPU machines.
#
# Keep this file focused on direct runtime dependencies only. Their transitive
# dependencies are resolved by pip and do not need to be pinned here.

--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.10.0+cu126
torchvision==0.25.0+cu126
numpy==1.26.4
# OpenCV >=4.12 requires NumPy 2.x; keep 4.11 for numpy==1.26.4 (matches jetson pins).
opencv-python==4.11.0.86
onnx==1.21.0
onnxruntime-gpu==1.23.2
pillow==12.1.1
pyyaml==6.0.3
requests==2.33.1
scipy==1.15.3
scikit-image==0.25.2
ultralytics==8.4.33
